apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "python-agri.fullname" . }}-test-connection"
  labels:
    {{- include "python-agri.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "python-agri.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
